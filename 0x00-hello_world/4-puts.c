#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        puts("\"Programming is like building a multilingual puzzle");
        return (0);
}

After you save the vi file and you’re on webterm you have to compile the file use command below to compile

gcc 4-puts.c -o 4-puts
